Join clause is most probably the clause which needs the most optimization as it deals with millions of tables to be joined with each other again and again so pretty much need to be very clever and try to optimise it at the on condition of the joins now cross join is a differnet thing but the rest of the stuff is pretty much covered by the on condition and we can optimise very much at that part so 

lets start with the point one itself taking all the things about the from the parser to a special data struct created for it okay so see the sturcture goes like see the from condition for each of the column is made and in that if the join occurs you can get it as a proper strucutre is present now one issue here persists is it kind fo works just on one join only multiple join it breaks up need to work on it so from name , join name and the on condition on condition can go like have tthe left stuff and the right stuff so it all is there and all of it handled by the from_parser_to_struct kind of the structure remains same for getting from parser to it so not elaborating on it but if intrested can read from the very first conversion in the main engines documentation 

similarly the if in the raw function solver join_func  remains the same as well except for the data structure changing 

okay so lets go with the fact that if the in the on condition if in the on condition consists of the = things like the a.id  = b.id something like that now here the best thing what we can do it hash out the columns as per one thing and then whichever of them matches do the join operation on it easy as that so pretty much is the thinking in here so now this one is covered in the function " join_equal_clause " here first all the hashh bins are created first which contain all the infornmation like the actual value the primary key of both the tables which are joining to each other and like number of entires and the row number and the table name stuff like that now why the primary key ? 
the main thinking behind it was see we get the columns which are similar and the campare values of it but for getting them to get our cursor to it we need to do the seek functions in the bytecodes and for it the tree needs to be sorted and trees are sorted as per the primary keys so the involvement of the primary keys is kind of important in here 
comming back to the main fucntiosn which handles this equal clause we pretty much need to like  get all the tables which are involved in the conditions and then intialise the cursor and move the cursor as per the condition is there now there is one speacial data structure also created in the select_info which contains all the infornmation about the number of tables all the hash stuff and yeah now we kind of tend to call this functions and then like check out that whatever the columns and the tables which are there needs to be called only once so hash is used here and in the main fucntion we tend to take out the tables which needs to joined and take the primary keys of it 
then the if tree is extending liek left or right of the root is present take it and then need to put in the hash we use the bytecode over here for that thing the same is done with the colname and the extra col is found 

comming on the bytecodes where all of these stuff is done exactly now in the hashing we pretty much put all the details in the blob and then pass it now in here number of hash bins a special formula dereived for it and then the the primary keys of both the stuff and then the row_num of the thing and the table_name of the campare thing where the data is there is picked from and then the hash curosr is been given in the bytecode and then the operation is done so yeah the whole blob is opened once we get the data we ask the function hash_bin_which this function just assigns the hash bin for the data regardless of any of the data type it contains and yeah this is how the whole hash table is been created for each of the stuff present in the each side of the = 
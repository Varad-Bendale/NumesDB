Join Clause 

Join clause is most probably the clause which needs the most optimization, as it deals with millions of tables being joined with each other again and again. So, we pretty much need to be very clever and try to optimize it at the `ON` condition of the joins. `CROSS JOIN` is a different thing, but the rest of the stuff is pretty much covered by the `ON` condition, and we can optimize a lot at that part.

So, let's start with the first point itself, taking everything from the parser to a special data structure created for it. The structure goes something like this: the `FROM` condition for each of the columns is made, and if a join occurs, we can get it as a proper structure since the structure is already present. One issue that currently persists is that it kind of works with only one join. When multiple joins are involved, it breaks, so that is something that needs to be worked on.

The structure basically contains the `FROM` name, the `JOIN` name, and the `ON` condition. The `ON` condition can further contain the left-side stuff and the right-side stuff. So, all of this is already handled by the `from_parser_to_struct` kind of structure. The structure itself remains the same for getting things from the parser to it, so I am not elaborating much on that here. If interested, you can read about it from the very first conversion in the main engine's documentation.

Similarly, the `if` in the raw function solver, `join_func`, remains pretty much the same as well, except for the data structure changing.

Now, let's consider the case where the `ON` condition consists of an `=` condition, something like `a.id = b.id`. Here, the best thing we can do is hash out the columns based on one thing, and then whichever of them matches, we perform the join operation on it. Pretty much, that is the thinking behind this approach.

This is covered in the function `join_equal_clause`. Here, first all the hash bins are created, which contain all the information like the actual value, the primary key of both the tables which are joining with each other, the number of entries, the row number, the table name, and other required information.

Now, why the primary key? The main thinking behind this was that we get the columns which are similar and the compare values of them, but to actually get to those values and move our cursor to them, we need to use the seek functions in the bytecodes. For that, the tree needs to be sorted, and trees are sorted according to the primary keys. So, the involvement of the primary keys becomes kind of important here.

Coming back to the main function which handles this equal clause, we pretty much need to get all the tables which are involved in the conditions, initialize the cursor, and move the cursor according to the condition that is present. There is also one special data structure created in `select_info`, which contains all the information about the number of tables, all the hash-related information, and other required details.

We then call these functions and make sure that whatever columns and tables are present need to be called only once. This is where the hash is used. In the main function, we take out the tables which need to be joined and take the primary keys of those tables. Then, if the tree is extending either to the left or right of the root, we take that information and put it into the hash. We use the bytecode for this operation. The same thing is done with the column name, and the extra column is found.

Coming to the bytecodes, this is where all of these things are actually done. In the hashing part, we pretty much put all the required details into the blob and then pass it. Here, the number of hash bins is calculated using a special formula. Then the primary keys of both sides, the row number, and the table name of the compare value are picked from the required data. The hash cursor is then given in the bytecode and the operation is performed.

Once we get the data, the whole blob is opened and we call the function `hash_bin_which`. This function simply assigns the hash bin for the data regardless of the data type it contains. This is how the whole hash table is created for each of the required things present on each side of the `=` condition.

'tables_and_their_hash_cursor_num' and 'tables_and_thier_cursor_num' these are the main responsible for getting the table numbers of the every table which are present in the exprtession so pretty much like the first we do the hash for the stuff and as per it we have the counter we just get the mod of the current table number  with the number of tables present and then give it 


Okay lets have some attention towards the inequality present in the on condiotion now normal hash really fails very badly here so need to like get some better appprach so the see we are playing with the ranges in here the most maximum number and the most minimum number in the row we track them we get the range of it and then operate on those ranges and at the very end we just get the ranges with respect to the tables which we are joining and then like we just move in that range only the function 'get_the_max_and_min' works the same thing what it does is takes all the tables and the columns which are presen tin the thing and then goes accross all the rows and find the minimum and the maximum and then store them easy as that loop over the tables get the min and max through the bytecodes aggregate functions and use it later on 

Okay stuff gets intresting from this function here 'take_care_of_expression' okay so the basic thinking in here is liek if the expression comes in the inequality any side we pretty much need to like get the ranges with respect to both the tables which we are going to join up with so we first give the target of the table in here we go to the entire leaf of the tree first and then as we start getting the tables and the thier columns we get the range first and then operate them and if the last table is remaining at the point we just need to like stop everything present in there and then like solve the inequality as per the table in that one and get the range for the table now one of the easiest approach could have been liek jsut trakc from the top and as soon as you get the very first table for the thing which hits for the target table number and solve with repect to it not quite the issue comes with the recursion you cant get the proper communiation in between and i kind of felt this was a eaiser manner than doing that so yeah the same stuff is done for the extra col and the normal columns and if just the normal column is present the recursion brings up the stuff and then work on it and also if the columns are not present just the plain data present we can also work for it as well now it pretty much gives up the the register 1 the register 2 which contains the answers and then if the extra col is present or not the binary variable for that is also there 

cool we got the expression now which we know exactly which side of the camparing function does the last one last so pretty much we pass that part of the tree and then take a map of the ery first table found and then use that map for reach to that one while solving the inequality 

one more side functipn is checxking if the range we have is negative or not cuase if the range comes in negative we pretty much need to chang ethe inequality sign in certain conditions two varients if expression 'check_if_the_stuff_negation_or_not' and for normal one 'check_if_the_stuff_negation_or_not_without_solver' 

now see we pretty much have the solved partial solved version ( range ) the map of the target which we have and the know which of the side of the inequality is the target lying so that way now the function 'found_the_target_to_be_deplished_solve_it' gives the final range for the target table so first checking which side the target lies if it is right we dont have the solved version of the left side so we need to solve that first and then move forward now ofc if the expression is there we call the fucntion or is only the normal data is there we directly get the data okay now we come to the main thing we earlier mapped out the entire of the route which is like the target table so now the we pretty much go in the loop they are in the 0 and 1 as the stuff is binary if the thing is on one side the opposite side is completly safe and can be directly solved and get the range of the thing ( if data normal range if equation the solved range ) and then we check if the thing is negative or what if negation in the cases of the * and / the campare function. inverts so we need to do it and then the range which is gotten is then transfered in the another side and solved it accordingly and get the range till we reach end of the tree finally we get the calculated range , the target table and column and the inequality and similarly the same functions are done for the left side just the finding what the data is on the right side we dont need to worry about cuase that data is passed in the function and solved similar way 

Now main mapping of everything is done in the function 'join_inequality_clause' here first all the tables are gotten hash si done we get the cursor numbers and then intialise all of them get the primary keys of both the target material  , the ranges of all the columns is called in here and then the main tree stump is in this part itself we first put the whole process presented on top for the left side and the right side of the tree and if the thing is there for the we just do the above process else just take the normal data if present and work on it and now once the whole thing is done we get the whole range of it we just check up the if the table and the column selected is the primary key or not cuase if the thing si primary key we dont need to do the operation gt_op for finding the exact stuff directly plain search o(1) and done .

